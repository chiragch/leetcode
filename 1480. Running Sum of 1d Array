class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        i = 1
        print(len(nums))
        while i<len(nums):
            nums[i] += nums[i-1]
            i+=1
        return nums
    
